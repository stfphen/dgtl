# DGTL OS — local config
# 1) Rename this file to  .env   (just ".env", no ".example")
# 2) Paste your Anthropic API key below (get one at https://console.anthropic.com)
# The key stays on your Mac, in this server only — it never reaches the browser.

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# Optional overrides:
# MODEL=claude-haiku-4-5-20251001
# PORT=8787
