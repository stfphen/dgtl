import { requireCoreApproval } from "../../../../../lib/permissions";import { getSessionTeamId } from "../../../../../lib/store";import { OutreachService } from "../../../../../lib/stage2/outreachService";import { redirectTo,stage2ErrorResponse } from "../../../../../lib/stage2/api";import { requireStage2Repository,stage2Actor } from "../../../../../lib/stage2/server";
export async function POST(request){try{const session=await requireCoreApproval();const teamId=getSessionTeamId(session);const service=new OutreachService({teamId,actor:stage2Actor(session),repository:requireStage2Repository()});const results=await service.processOutbox({limit:50});return redirectTo(request,"/operations/outbox",`Test worker processed ${results.length} messages. No external email was sent.`);}catch(error){return stage2ErrorResponse(error,request);}}
