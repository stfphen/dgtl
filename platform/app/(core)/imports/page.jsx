import Link from "next/link";
import { DateText, PageHeader, Status } from "../../../components/core/CoreUi";
import { getStage2PageContext } from "../../../lib/stage2/server";
export const dynamic="force-dynamic";
export default async function ImportsPage({searchParams}){const params=await searchParams;const {imports}=await getStage2PageContext();const batches=imports?await imports.listBatches():[];return <div className="core-page"><PageHeader title="Prospect imports" description="Upload → stage → map → normalize → detect duplicates → review → approved import. No spreadsheet writes directly to canonical records."/>{params?.notice?<p className="core-notice">{params.notice}</p>:null}<div className="core-toolbar"><span className="core-count">{batches.length}</span><Link className="core-button is-primary" href="/imports/new">New import</Link></div><div className="core-table-wrap"><table className="core-table"><thead><tr><th>Source</th><th>Status</th><th>Rows</th><th>Conflicts</th><th>Uploaded</th></tr></thead><tbody>{batches.length?batches.map(batch=><tr key={batch.id}><td><span className="core-table__primary"><Link href={`/imports/${batch.id}`}>{batch.sourceFilename}</Link><small>{batch.sourceType} · {batch.id}</small></span></td><td><Status value={batch.status}/></td><td>{batch.appliedRows||0} / {batch.totalRows||0}</td><td>{batch.conflictingRows||0}</td><td><DateText value={batch.uploadedAt}/></td></tr>):<tr><td colSpan="5">{imports?"No staged imports yet.":"DATABASE_URL is not configured; routed reads remain available, but Stage 2 writes are disabled."}</td></tr>}</tbody></table></div></div>}
