import Link from "next/link";
import { DateText, PageHeader, Status } from "../../../components/core/CoreUi";
import { getStage3PageContext } from "../../../lib/stage3/server";
export const dynamic="force-dynamic";
export default async function GenerationJobsPage(){const{automation}=await getStage3PageContext();const jobs=automation?await automation.listJobs():[];return <div className="core-page"><PageHeader title="Generation jobs" description="Immutable context snapshots, bounded DGTL skill adapters, validation, review, and deployment intent."/><div className="core-table-wrap"><table className="core-table"><thead><tr><th>Job</th><th>Adapter</th><th>Status</th><th>Slug</th><th>Created</th></tr></thead><tbody>{jobs.length?jobs.map(job=><tr key={job.id}><td><span className="core-table__primary"><Link href={`/generation-jobs/${job.id}`}>{job.artifactKind||"asset"}</Link><small>{job.id}</small></span></td><td>{job.adapterId||job.requestedSkill}</td><td><Status value={job.status}/></td><td>{job.slug||"—"}</td><td><DateText value={job.createdAt}/></td></tr>):<tr><td colSpan="5">{automation?"No generation jobs yet.":"DATABASE_URL is not configured; Stage 3 is read-only."}</td></tr>}</tbody></table></div></div>}
