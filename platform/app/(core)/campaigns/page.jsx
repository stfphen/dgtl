import Link from "next/link";import { DateText,PageHeader,Status } from "../../../components/core/CoreUi";import { getStage2PageContext } from "../../../lib/stage2/server";
export const dynamic="force-dynamic";
export default async function CampaignsPage({searchParams}){const params=await searchParams;const {outreach}=await getStage2PageContext();const campaigns=outreach?await outreach.listCampaigns():[];return <div className="core-page"><PageHeader title="Campaigns" description="Canonical opportunity/contact cohorts, structured personalization, immutable approvals, and durable test delivery."/>{params?.notice?<p className="core-notice">{params.notice}</p>:null}<div className="core-toolbar"><span className="core-count">{campaigns.length}</span><Link className="core-button is-primary" href="/campaigns/new">New campaign</Link></div><div className="core-table-wrap"><table className="core-table"><thead><tr><th>Campaign</th><th>Status</th><th>Approval</th><th>Owner</th><th>Updated</th></tr></thead><tbody>{campaigns.length?campaigns.map(c=><tr key={c.id}><td><span className="core-table__primary"><Link href={`/campaigns/${c.id}`}>{c.name}</Link><small>{c.id}</small></span></td><td><Status value={c.status}/></td><td><Status value={c.approvalState}/></td><td>{c.ownerUserId||"—"}</td><td><DateText value={c.updatedAt}/></td></tr>):<tr><td colSpan="5">{outreach?"No canonical campaigns yet.":"DATABASE_URL is not configured; Stage 2 writes are disabled."}</td></tr>}</tbody></table></div></div>}
